# Release Notes

## v1.0.0 — 2026-02-11
Stable, deterministic, contract-locked release.

- Snapshot schema locked (v1) with enforced schema_version.
- Explain schema v1 introduced and locked.
- Deterministic replay/tune outputs with golden locks.
- Policy invariant layer enforced via CI tests.
